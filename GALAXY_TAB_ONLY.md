# 갤럭시 탭만으로 APK 만들기

이 프로젝트는 PC 없이 갤럭시 탭의 인터넷 브라우저만으로 APK를 만들 수 있도록 GitHub Actions 빌드 파일을 포함한다.

## 가장 간단한 절차
1. 갤럭시 탭에서 GitHub에 로그인하고 새 저장소를 만든다.
2. 이 ZIP을 `내 파일`에서 압축 해제한다.
3. 저장소의 `Add file` → `Upload files`에서 압축을 푼 폴더 안의 파일과 폴더를 업로드한다. `.github` 폴더도 반드시 포함한다.
4. 업로드 후 저장소 상단의 `Actions`를 연다.
5. 왼쪽에서 `Build Galaxy Tab APK`를 선택한다.
6. `Run workflow` → `Run workflow`를 누른다.
7. 빌드가 끝나고 초록색 체크가 뜨면 해당 실행 기록을 연다.
8. 아래 `Artifacts`의 `doodle-cat-study-pet-apk`를 내려받는다.
9. 받은 ZIP을 압축 해제하면 `app-debug.apk`가 나온다.
10. 갤럭시 탭에서 해당 APK를 열어 설치한다. 안드로이드가 브라우저/내 파일의 앱 설치 권한을 요구하면, 자신이 직접 빌드한 이 APK에 한해서 허용한다.

## 첫 실행
- `다른 앱 위에 표시` 권한을 허용해야 고양이가 삼성노트, PDF, 유튜브 등 다른 앱 위에도 뜬다.
- 알림 권한이 나오면 허용하는 편이 좋다. 오버레이 서비스가 계속 실행 중임을 표시하는 데 사용한다.
- 공부 모드에서 25분/50분 집중 타이머와 10분 휴식을 선택할 수 있다.

## v0.2 기능
- 화면 위 낙서 고양이
- 고양이 드래그 이동
- 자동 배회/앉기/잠자기/랜덤 반응
- 밥 주기
- 털실공 놀이
- 25분/50분 집중 및 10분 휴식
- 공부 중 책상에 앉아 있는 고양이 상태
- 오늘 공부 시간 저장
- 수능 D-Day 표시

## 참고
GitHub Actions가 실제 Android SDK와 Gradle을 클라우드에서 실행하므로 태블릿에 Android Studio를 설치할 필요가 없다.
